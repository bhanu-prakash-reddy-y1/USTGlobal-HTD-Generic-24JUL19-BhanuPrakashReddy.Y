import { MyDecorativeDirective } from './my-decorative.directive';

describe('MyDecorativeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDecorativeDirective();
    expect(directive).toBeTruthy();
  });
});
